#!/usr/bin/node
// Write a class Rectangle that defines a rectangle
module.export = class Rectangle {
    constructor(w, h) {
        this.height = h;
        this.width = w;
    }

    if (w && h === 0){
        const rec = new();
    }
};